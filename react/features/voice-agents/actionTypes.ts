/** Updates the known set of voice agents (mirrored from room metadata). */
export const SET_VOICE_AGENTS = 'SET_VOICE_AGENTS';

/** Records the local participant's consent decision for receiving a voice agent's media. */
export const SET_VOICE_AGENT_CONSENT = 'SET_VOICE_AGENT_CONSENT';

/** Records whether a voice agent is currently speaking (synthetic source sending). */
export const SET_VOICE_AGENT_SPEAKING = 'SET_VOICE_AGENT_SPEAKING';
