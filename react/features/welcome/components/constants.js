// @flow

import BaseTheme from '../../base/ui/components/BaseTheme';


export const drawerContentOptions = {
    activeBackgroundColor: BaseTheme.palette.ui12,
    activeTintColor: BaseTheme.palette.ui12,
    labelStyle: {
        marginLeft: BaseTheme.spacing[2]
    }
};
